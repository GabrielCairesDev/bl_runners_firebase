import 'package:flutter/material.dart';

class PaginaConcluirRegistro extends StatelessWidget {
  const PaginaConcluirRegistro({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Concluir Cadastro'),
      ),
    );
  }
}
