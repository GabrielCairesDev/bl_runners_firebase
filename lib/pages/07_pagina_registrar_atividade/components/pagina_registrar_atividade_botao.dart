import 'package:flutter/material.dart';

class PaginaRegistrarBotao extends StatelessWidget {
  const PaginaRegistrarBotao({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      onPressed: () => {},
      icon: const Icon(Icons.save),
    );
  }
}
