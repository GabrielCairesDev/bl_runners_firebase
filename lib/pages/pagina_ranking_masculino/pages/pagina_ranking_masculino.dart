import 'package:flutter/material.dart';

class PaginaRankingMasculino extends StatelessWidget {
  const PaginaRankingMasculino({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(extendBodyBehindAppBar: true, appBar: AppBar(title: const Text('Ranking Masculino')));
  }
}
